type Query {
  info: String!
  user(id: ID!): User!
}

type Mutation {
  signup(email: String!, password: String!): String!
  login(email: String!, password: String!): String!
}

type User {
	id: ID!
  email: String!
  password: String!
}